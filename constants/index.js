const { BASE_SPOTIFY_URL, TOKEN_YT } = require('./api');
const { MUSIC_CHANNEL, MUSIC_LOG, TOKEN } = require('./discord');

const CONSTANTS = {
  BASE_SPOTIFY_URL,
  MUSIC_CHANNEL,
  MUSIC_LOG,
  TOKEN_YT,
  TOKEN,
  YOUTUBE_SEARCH_OPTIONS
}

module.exports = CONSTANTS